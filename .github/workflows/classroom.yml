name: GitHub Classroom Workflow

on: [push]

permissions:
  checks: write
  actions: read
  contents: read

jobs:
  build:
    name: Autograding
    runs-on: ubuntu-latest
    steps:
      - name: Set-up for grading
        uses: ImperialCollegeLondon/y2cl-setup
        with:
          project: A
          src_dir: raytracer
      - uses: education/autograding@v1
